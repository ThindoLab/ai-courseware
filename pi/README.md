# pi/ · Agent 层（给人审）

| 文件 | 作用 |
|------|------|
| `教练.md` | **教练手册**：排程、分工、工具分配、收工 |
| `memory/` | 产品原则、教训 |
| `tools/README.md` | 工具导航：哪些已注入 |
| `tools/*.ts` | 工具实现 |
| `samples/portraits.json` | 精品短画像 |
| `ima/` | 知识库目录、检索指南、catalog；`npm run ima:sync` |

角色认定：`pi/角色/`（含 `图解.md` 2D/伪3D/过程动画）。`.pi/agents` 链到此处。  
`teams.yaml` 只是花名册；出场顺序只写在 `教练.md`。
