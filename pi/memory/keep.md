# 保留清单（当前基线）

当前精品库整体好于上一版。没有新的用户确认，**禁止**对下列条目做整页重写 / `CREATE_FORCE_LLM`。
质检加严只约束**以后的** `/create`，不回头改这些 HTML。

## 整篇锁住

| slug | 锁什么 |
|------|--------|
| `average` | 均分图解。用户评为可用。 |
| `there-be` | 通关祝贺页。其他收束对照本篇。 |
| `compare-length` | 比长短。用户评为可用（v4）。 |
| `clock-read` | 认钟整篇。用户评为可用（v3）。演示=正式玩同一只钟；指针分拍讲清；选项从当前时刻生成。 |
| `fraction` | 分披萨 v3。用户评中等、暂时这样。 |
| `stranger-door` | 敲门 v3。逻辑可用；动画后置，不为此整页重跑。 |
| `pattern-lights` | v7 彩灯找规律。用户评为可用。夜空横串+小狐狸；揭晓镂空环（引导和玩同一套）；禁止实心✓牌、禁止改成竖串。 |
| `color-sort` | v8 小鱼回家。用户评为可用（小鱼和颜色都很不错）。锁皮、颜色=判定、鱼铺开会游、拖在塘上。禁止再换场景。 |

## 局部锁住（以后若必须动，只改未锁部分）

| slug | 必须留下 |
|------|----------|
| `brush-steps` | 开场演示 + 庆祝 |
| `memory-match` | 翻牌动效（改选中态时不要拆掉翻转） |
| `tree-planting` | 动画整体风格（改树/段重叠和新增配色时不要换皮） |
| `chicken-rabbit` | 仅 v1 的腿数变化动效方向；当前 v2 残页不算基线 |
| `palm-measure` | 手掌一排量过去的动画方向；不要整页换皮 |
| `odd-even` | 两两拉手找落单的规则；换形象时不要改成分数题 |

## 写手纪律

- 主题命中上表时：先 `read` 现稿，保留锁住的结构，禁止为了「对齐交互」重画开场。
- 新篇对照 `there-be` 写结束页，对照本表已认好的动画学结构，不要整页抄走题面。
- 写出时带 `<!-- aid-contract ... -->`（见 `writer.md`），让 `qa_check` 能验空位 ⊆ 候选。
